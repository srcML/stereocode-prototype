static void myClass::updateGuess1(std::vector<Rate>& data,
				 Rate level,
				 Size i) {
  data[i] = level;
}


static void myClass::updateGuess2(std::vector<Rate>& data,
				 Rate level,
				 Size i) {
  data = level;
}


static void myClass::updateGuess3(std::vector<Rate>& data,
				 Rate level,
				 Size i) {

  int stuff;

  stuff = level;
}


static void myClass::updateGuess4(std::vector<Rate>& data,
				 Rate level,
				 Size i) {

  int stuff;

  stuff[i] = level;
}


static void myClass::updateGuess5(std::vector<Rate>& data,
				 Rate level,
				 Size i) {

  int stuff = level;
}


static void myClass::updateGuess6(std::vector<Rate>& data,
				 Rate level,
				 Size i) {

  Object stuff = level;
}

